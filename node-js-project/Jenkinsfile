pipeline {

   agent {

//       docker {
       label 'demo-docker'
       // image 'jenkins/ssh-agent'
        //  }  
    }

stages {
        stage('Build') {
            steps {
                script {
                sh 'hostname'
                sh 'ip a'
                echo 'Hello, this is the Build stage!'
            }
          }
        }
    }
}
