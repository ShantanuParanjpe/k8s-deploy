pipeline {

  agent {
       docker {
          label 'docker-slave'
          image 'bibinwilson/jenkins-slave:latest'
          } 
    }

stages {
        stage('Build') {
            steps {
                echo 'Hello, this is the Build stage!'
            }
        }
    }
}
