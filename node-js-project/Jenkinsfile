pipeline {

   agent any

      /* dockerContainer {
         label 'demo-docker'
          image 'jenkins/ssh-agent'
          }  
    } */

stages {
        stage('Build') {
            steps {
                echo 'Hello, this is the Build stage!'
            }
        }
    }
}
