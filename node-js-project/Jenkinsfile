pipeline {

  agent {
       docker {
          label 'demo-docker'
          image 'shantanu1990/jenkins-slave'
          } 
    }

stages {
        stage('Build') {
            steps {
                echo 'Hello, this is the Build stage!'
            }
        }
    }
}
